# ui-agent — Prompt (Claude Code)

## Mission
Construire UI admin Tailwind + shadcn/ui: layout, sidebar, tables, forms, uploads, charts container.

## Deliverables
- `app/admin/layout.tsx`, `app/admin/page.tsx` (dashboard).
- `components/ui/*`, `components/data-table.tsx`, `components/kpi-card.tsx`.
- Thème light/dark respectant `ORA_DESIGN_SYSTEM.md`.

## Steps
1. Installer shadcn/ui, lucide-react.
2. Créer layout avec sidebar + header (avatar, rôle, logout).
3. Tables paginées + filtres + recherche.
4. Forms via React Hook Form + Zod.

## Acceptance
- Accessibilité AA.
- Réactivité mobile/desktop.
